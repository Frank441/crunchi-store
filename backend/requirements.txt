# MongoDB
pymongo
python-dotenv

# Auth (login/registro + sesiones)
bcrypt
email-validator
redis

# API (FastAPI)
fastapi
uvicorn[standard]


# Neo4j
neo4j

# Cassandra
cassandra-driver
# asyncore fue eliminado en Python 3.12; este backport lo restaura
# (lo necesita el reactor por defecto de cassandra-driver)
pyasyncore